name: Prettier
on: push
jobs:
  Prettier:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: creyD/prettier_action@v4.3
        with:
          dry: True
          prettier_options: "-c **/*.{js,ts,json,yaml,md}"
